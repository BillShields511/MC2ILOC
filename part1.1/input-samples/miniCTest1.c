// int __line_comment_var__;
/* int __block_comment_var__;
   bool __block2; */
int g(int a, int b) {
  bool u;
  char v;
  int w[2] = {1, 2};
  int k = 0;
  u = true;
  v = '\\';
  if ((a != b) && (((a == b) != u) || !u) && ((a < b) || (a <= b)) && (a > -1) && (b >= 0)) {
    while (a < b) {
      (a = a + 1);
      (w[0] = (w[1] * 2) / 2 % 2);
      (k = k + 1);
      ;
    }
  } else {
    (v = '\'');
    if (!u) {
      (k = k);
    }
  }
  return a + w[0] + (-b) + k + v;
}
int main() {
  int x = 0;
  bool y;
  char z;
  x = g(0, 1);
  y = false;
  z = 'a';
  if (y || (z == '\0')) {
    (x = x + 1);
  }
  (x = (x - 0));
  return x;
}
